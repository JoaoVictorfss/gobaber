const { resolve } = require("path");

module.exports = {
  config: resolve(__dirname, "src", "config", "database.js"), //caminho para o arquivo de database
  "models-path": resolve(__dirname, "src", "app", "models"), //Caminho para a pasta de models
  "migrations-path": resolve(__dirname, "src", "database", "migrations"), //caminho para a pasta de migrations
  "senders-path": resolve(__dirname, "src", "database", "seeds"), //caminho para a pasta de seeds
};
